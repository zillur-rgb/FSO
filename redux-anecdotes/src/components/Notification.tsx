import React from 'react';

const Notification = () => {
  const style = {
    border: "solid",
    padding: 10,
    borderWidth: 1,
  };
  return <h1>Notification</h1>
};

export default Notification;
